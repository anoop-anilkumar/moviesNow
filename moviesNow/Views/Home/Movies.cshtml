@model IEnumerable<moviesNow.Models.Movies>

    @{
        ViewBag.Title = "Movies";
    }

    <h2>Movies</h2>

    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Director)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DateRelease)
            </th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Id)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Director)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.DateRelease)
                </td>
            </tr>

        }
    </table>

@*
    @model IEnumerable<moviesNow.Models.Movies>
        @{
            ViewBag.Title = "Movies";
        }

        <h2>Movies</h2>
        <div class="row">
            <div class="col-md-12">
                <div class="col-md-3">
                    @Html.LabelFor(model => model.Id)
                    @Html.TextBoxFor(m => m.Id)
                </div>
                <div class="col-md-3">
                    @Html.LabelFor(m => m.Name)
                    @Html.TextBoxFor(m => m.Name)
                </div>
                <div class="col-md-3">
                    @Html.LabelFor(m => m.Director)
                    @Html.TextBoxFor(m => m.Director)
                </div>
                <div class="col-md-3">
                    @Html.LabelFor(m => m.DateRelease)
                    @Html.TextBoxFor(m => m.DateRelease)
                </div>
            </div>
        </div>*@


@*@foreach (var data in Model)
    {
    <div>@data.Id,@data.Name,@data.Director,@data.DateRelease</div>
    <br />
    }*@